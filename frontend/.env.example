# ==============================================================================
# Vercel Environment Variables (Frontend)
# ==============================================================================
# The public HTTPS URL of your Render backend deployment (no trailing slash)
VITE_API_URL=https://your-backend-app.onrender.com

# Optional (only needed if your WebSocket server is on a different custom host)
# VITE_SOCKET_URL=https://your-backend-app.onrender.com
